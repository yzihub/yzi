import { applyProps } from './applyProps.mjs';
import { log } from './log.mjs';
import { prepareUpdate } from './prepareUpdate.mjs';

"use strict";
function commitUpdate(instance, type, oldProps, newProps) {
  log("info", "lifecycle::commitUpdate");
  const diff = prepareUpdate(
    instance,
    type,
    oldProps,
    newProps
  );
  if (diff) {
    applyProps(instance, diff);
  }
}

export { commitUpdate };
//# sourceMappingURL=commitUpdate.mjs.map
