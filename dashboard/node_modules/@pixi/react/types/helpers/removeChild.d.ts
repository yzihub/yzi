import { type HostConfig } from '../typedefs/HostConfig';
/** Removes elements from our scene and disposes of them. */
export declare function removeChild(_parentInstance: HostConfig['containerInstance'], childInstance: HostConfig['instance']): void;
