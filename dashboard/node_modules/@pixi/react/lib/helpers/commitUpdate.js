'use strict';

var applyProps = require('./applyProps.js');
var log = require('./log.js');
var prepareUpdate = require('./prepareUpdate.js');

"use strict";
function commitUpdate(instance, type, oldProps, newProps) {
  log.log("info", "lifecycle::commitUpdate");
  const diff = prepareUpdate.prepareUpdate(
    instance,
    type,
    oldProps,
    newProps
  );
  if (diff) {
    applyProps.applyProps(instance, diff);
  }
}

exports.commitUpdate = commitUpdate;
//# sourceMappingURL=commitUpdate.js.map
